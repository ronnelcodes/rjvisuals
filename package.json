{
  "name": "rj-visuals-netlify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=20" },
  "devDependencies": { "netlify-cli": "^23.5.0" }
}

